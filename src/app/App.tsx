import './App.scss';
import { TodoList } from '../widgets/TodoList/TodoList';

const App = () => {
  return (
    <div className="container">
      <TodoList />
    </div>
  );
};

export default App;
